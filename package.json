{
  "name": "gcs-eevee-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "imports": {
    "#shared/*": "./shared/*.ts"
  },
  "scripts": {
    "dev": "eve dev",
    "dev:assistant": "eve dev --agent assistant",
    "dev:monitor": "eve dev --agent monitor --port 2001",
    "dev:all": "npx vercel@59.23.2 dev --local --listen 3300",
    "build": "eve build",
    "build:assistant": "eve build --agent assistant",
    "deploy": "eve deploy",
    "start": "eve start --agent assistant",
    "start:monitor": "eve start --agent monitor --port 3001",
    "eval": "eve eval --agent assistant",
    "typecheck": "tsc"
  },
  "dependencies": {
    "@ai-sdk/google": "^4.0.67",
    "@ai-sdk/openai": "^4.0.65",
    "@toon-format/toon": "^4.1.1",
    "@vercel/connect": "2.2.0",
    "ai": "^7.0.105",
    "eve": "^0.63.0",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@types/node": "24.x",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": "24.x"
  }
}
